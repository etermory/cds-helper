<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:views="clr-namespace:CdsHelper.Main.UI.Views"
                    xmlns:units="clr-namespace:CdsHelper.Support.UI.Units;assembly=CdsHelper.Support">

    <Style TargetType="{x:Type views:CityContent}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type views:CityContent}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- 도시 검색 필터 -->
                        <WrapPanel Grid.Row="0" Margin="10" Orientation="Horizontal">
                            <TextBlock Text="도시명 검색:" VerticalAlignment="Center" Margin="0,0,5,0" />
                            <TextBox Width="150" Height="25" VerticalAlignment="Center" Margin="0,0,10,0"
                                     Text="{Binding CityNameSearch, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock Text="문화권:" VerticalAlignment="Center" Margin="10,0,5,0" />
                            <ComboBox Width="120" Height="25" VerticalAlignment="Center" Margin="0,0,10,0"
                                      ItemsSource="{Binding CulturalSpheres}"
                                      SelectedItem="{Binding SelectedCulturalSphere}" />

                            <CheckBox Content="도서관 있는 도시만" VerticalAlignment="Center" Margin="10,0,10,0"
                                      IsChecked="{Binding LibraryOnly}" />

                            <CheckBox Content="조선소 있는 도시만" VerticalAlignment="Center" Margin="10,0,10,0"
                                      IsChecked="{Binding ShipyardOnly}" />

                            <Button Content="필터 초기화" Width="100" Height="30" Margin="10,0,0,0"
                                    Command="{Binding ResetCityFilterCommand}" />

                            <units:ExportButton Content="데이터 내보내기"
                                    Command="{Binding ExportCitiesToJsonCommand}" />
                        </WrapPanel>

                        <!-- 도시 데이터 그리드 -->
                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding Cities}"
                                  SelectedItem="{Binding SelectedCity}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  Margin="10"
                                  AlternatingRowBackground="LightGray">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                                <DataGridTextColumn Header="도시명" Binding="{Binding Name}" Width="150" />
                                <DataGridTextColumn Header="문화권" Binding="{Binding CulturalSphere}" Width="120" />
                                <DataGridTextColumn Header="위도" Binding="{Binding LatitudeDisplay}" Width="80" />
                                <DataGridTextColumn Header="경도" Binding="{Binding LongitudeDisplay}" Width="80" />
                                <DataGridCheckBoxColumn Header="도서관" Binding="{Binding HasLibrary}" Width="70" />
                                <DataGridCheckBoxColumn Header="조선소" Binding="{Binding HasShipyard}" Width="70" />
                                <DataGridTextColumn Header="PixelX" Binding="{Binding PixelX}" Width="70" />
                                <DataGridTextColumn Header="PixelY" Binding="{Binding PixelY}" Width="70" />
                                <DataGridTemplateColumn Header="수정" Width="60">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="수정"
                                                    Command="{Binding DataContext.EditCityPixelCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
